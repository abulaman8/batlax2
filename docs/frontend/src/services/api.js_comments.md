# Comments extracted from api.js

- Line 3: // Create an Axios instance with the base URL of our Flask API
- Line 11: // Request Interceptor
- Line 12: // This function runs before every request is sent.
- Line 13: // It checks if a token exists in localStorage and adds it to the Authorization header.
