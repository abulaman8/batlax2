# Comments extracted from index.js

- Line 36: // Navigation Guard
- Line 37: // Runs before every route change to check authentication
- Line 44: // Redirect to login if not authenticated
- Line 47: // Redirect if role doesn't match (e.g., patient trying to access admin)
